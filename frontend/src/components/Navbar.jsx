import React from "react";

const Navbar = () => (
  <nav className="navbar navbar-dark bg-dark px-3">
    <span className="navbar-brand mb-0 h1">Internship Task Dashboard</span>
  </nav>
);

export default Navbar;
